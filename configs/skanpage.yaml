document: feedback-pipeline-workload
version: 1
data:
  name: skanpage
  description: Fedora org.kde.skanpage flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak-kde6-app
  packages: []
  arch_packages:
    aarch64:
      - skanpage
      - sane-backends-flatpak-config
    x86_64:
      - skanpage
      - sane-backends-flatpak-config
    ppc64le:
      - skanpage
      - sane-backends-flatpak-config
