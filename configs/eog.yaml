document: feedback-pipeline-workload
version: 1
data:
  name: eog
  description: Fedora org.gnome.eog flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak-app
  packages:
    - eog
    - eog-plugins
