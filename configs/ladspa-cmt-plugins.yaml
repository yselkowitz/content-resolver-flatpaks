document: feedback-pipeline-workload
version: 1
data:
  name: ladspa-cmt-plugins
  description: Fedora org.fedoraproject.LinuxAudio.Plugins.CMT flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak-runtime
  packages:
    - ladspa-cmt-plugins
    # cleanup-commands (already in runtime)
    - coreutils-single
