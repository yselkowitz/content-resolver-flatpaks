document: feedback-pipeline-workload
version: 1
data:
  name: file-roller
  description: Fedora org.gnome.FileRoller flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak
  packages:
    - file-roller
    - arj
    - genisoimage
    - lzop
    - p7zip-plugins
    - rzip
    - unar
    # libraries in runtime, programs missing: brotli
    # programs in runtime: bzip2, cpio, gzip, lz4, lzip, lzma, ncompress, tar, unzip, xz, zip, zstd
    # not available in Fedora: lha, lrzip, rar, unace, unalz, unsquashfs, unstuff, zoo
    # not optimal in Fedora: unrar(-free) [limited support compared to proprietary unrar, other options handle RAR better]
