document: feedback-pipeline-workload
version: 1
data:
  name: typescript
  description: Fedora org.fedoraproject.Sdk.Extension.typescript flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak-sdk
  packages:
    - typescript
    - typescript-flatpak-config
    # for cleanup-commands (part of runtime)
    - coreutils-single
