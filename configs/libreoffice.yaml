document: feedback-pipeline-workload
version: 1
data:
  name: libreoffice
  description: Fedora org.libreoffice.LibreOffice flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak
  packages:
    - libreoffice
    - libreoffice-base
    - libreoffice-calc
    - libreoffice-draw
    - libreoffice-impress
    - libreoffice-math
    - libreoffice-writer
    # extensions
    - libreoffice-emailmerge
    - libreoffice-filters
    - libreoffice-librelogo
    #        - libreoffice-nlpsolver
    - libreoffice-ogltrans
    - libreoffice-postgresql
    #        - libreoffice-wiki-publisher
    # integration plugins
    - libreoffice-gtk3
    - libreoffice-x11
    # for texlive extension
    - libgs
    # third-party extensions

    #        - libreoffice-gallery-vrt-network-equipment
    #        - libreoffice-grammalecte
    #        - libreoffice-TexMaths
    #        - libreoffice-voikko
    #        - openoffice.org-diafilter
    # langpacks
    - libreoffice-langpack-*
