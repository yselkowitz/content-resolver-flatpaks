document: feedback-pipeline-workload
version: 1
data:
  name: cantor
  description: Fedora org.kde.cantor flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak-kde6-app
  packages: []
  arch_packages:
    x86_64:
      - cantor
      - cantor-R
      # soft dependencies
      - gnuplot
      - maxima
      - octave
      - python3-numpy
      - qalculate
    aarch64:
      - cantor
      - cantor-R
      # soft dependencies
      - gnuplot
      - maxima
      - octave
      - python3-numpy
      - qalculate
    ppc64le:
      - cantor
      - cantor-R
      # soft dependencies
      - gnuplot
      - maxima
      - octave
      - python3-numpy
      - qalculate
