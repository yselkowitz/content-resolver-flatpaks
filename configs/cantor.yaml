document: feedback-pipeline-workload
version: 1
data:
  name: cantor
  description: Fedora org.kde.cantor flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak
  packages: []
  arch_packages:
    x86_64:
      - cantor
      # soft dependencies
      - gnuplot
      - maxima
      - python3-numpy
      - python3-plotly
      - qalculate
    aarch64:
      - cantor
      # soft dependencies
      - gnuplot
      - maxima
      - python3-numpy
      - python3-plotly
      - qalculate
    ppc64le:
      - cantor
      # soft dependencies
      - gnuplot
      - maxima
      - python3-numpy
      - python3-plotly
      - qalculate
