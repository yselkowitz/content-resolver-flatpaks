document: feedback-pipeline-workload
version: 1
data:
  name: lokalize
  description: Fedora org.kde.lokalize flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak-kde6-app
  packages:
    - lokalize
    # soft dependencies
    - poxml
