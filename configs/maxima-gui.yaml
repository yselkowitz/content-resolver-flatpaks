document: feedback-pipeline-workload
version: 1
data:
  name: maxima-gui
  description: Fedora net.sourceforge.maxima.xmaxima flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak-app
  packages:
    - maxima-gui
    - gnuplot-wx
