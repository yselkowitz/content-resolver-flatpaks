document: feedback-pipeline-workload
version: 1
data:
  name: ladspa-swh-plugins
  description: Fedora org.fedoraproject.LinuxAudio.Plugins.swh flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak
  packages:
    - ladspa-swh-plugins
    - lv2-swh-plugins
    # cleanup-commands (already in runtime)
    - coreutils-single
