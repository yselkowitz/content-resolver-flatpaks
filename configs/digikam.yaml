document: feedback-pipeline-workload
version: 1
data:
  name: digikam
  description: Fedora org.kde.digikam flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak-kde6-app
  packages: []
  arch_packages:
    x86_64:
      - digikam
      # soft dependencies
      - hugin-base
      - perl-Image-ExifTool
      - sane-backends-flatpak-config
    aarch64:
      - digikam
      # soft dependencies
      - hugin-base
      - perl-Image-ExifTool
      - sane-backends-flatpak-config
    ppc64le:
      - digikam
      # soft dependencies
      - hugin-base
      - perl-Image-ExifTool
      - sane-backends-flatpak-config
