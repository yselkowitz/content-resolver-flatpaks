document: feedback-pipeline-workload
version: 1
data:
  name: qutebrowser
  description: Fedora org.qutebrowser.qutebrowser flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak-kde6-app
  packages: []
  arch_packages:
    aarch64:
      - qutebrowser
      # soft dependencies
      - python3-pygments
    x86_64:
      - qutebrowser
      # soft dependencies
      - python3-pygments
    ppc64le:
      - qutebrowser
      # soft dependencies
      - python3-pygments
