document: feedback-pipeline-workload
version: 1
data:
  name: hexchat
  description: Fedora io.github.Hexchat flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak-app
  packages:
    - hexchat
    # optional plugins
    - hexchat-autoaway
    # for lua extension scripts
    - lua-lgi
    - gtk2-flatpak-config
