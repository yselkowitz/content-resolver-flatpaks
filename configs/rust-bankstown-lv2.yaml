document: feedback-pipeline-workload
version: 1
data:
  name: rust-bankstown-lv2
  description: Fedora org.fedoraproject.LinuxAudio.Plugins.Bankstown flatpak
  maintainer: flatpak-sig
  labels:
    - flatpak-runtime
  packages: []
  arch_packages:
    x86_64:
      - lv2-bankstown
      # cleanup-commands (already in runtime)
      - coreutils-single
    aarch64:
      - lv2-bankstown
      # cleanup-commands (already in runtime)
      - coreutils-single
