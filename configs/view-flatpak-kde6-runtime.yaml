---
document: feedback-pipeline-compose-view
version: 1
data:
  name: Fedora KDE Flatpak Runtime
  description: Packages included in the Fedora KDE Flatpak runtime.
  maintainer: flatpak-sig
  labels:
  - flatpak-kde6-runtime
  repository: repository-fedora-flatpak-runtime
  buildroot_strategy: none

  # Packages to be flagged as unwanted  on specific architectures
  #unwanted_arch_packages:
  #  x86_64:
  #  - arch-specific-package
  #  aarch64:
  #  - another-one

  # SRPMs (components) to be flagged as unwanted
  unwanted_source_packages: []
